name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy app
        run: |
          cd /root/devops-project
          git pull
          docker-compose down
          docker-compose up -d --build
